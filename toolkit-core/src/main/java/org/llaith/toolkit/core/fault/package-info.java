/**
 Faults are used to model warnings and errors during long running processes
 that involved working on sets of resources that are excluded on such faults
 rather than aborting the entire process.

 @author Nos Doughty
 @version 1.0
 */
@Maturity(BETA)
package org.llaith.toolkit.core.fault;

import org.llaith.toolkit.common.intention.Maturity;

import static org.llaith.toolkit.common.intention.MaturityLevel.BETA;
