/**
 Dtos are objects that support change-tracking internally,
 have a built-in builder mode, and can be stored in a session
 and synchronize state between instances that represent the
 same identity.
 @author Nos Doughty
 @version 1.0
 */
@Maturity(ALPHA)
package org.llaith.toolkit.core.dto;

import org.llaith.toolkit.common.intention.Maturity;

import static org.llaith.toolkit.common.intention.MaturityLevel.ALPHA;
